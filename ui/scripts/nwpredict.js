(function($) {
  $(function() {
    $('#inputbox').keypress(function(e) {
      if (e.key == ' ' || e.key == 'Tab' || e.key == 'Enter') {
        // blank inserted
        // Process suggestions
        
      }
    });
  });
})(jQuery);
